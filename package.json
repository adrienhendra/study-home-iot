{
  "dependencies": {
    "firebase": "^4.12.1",
    "jshint": "^2.9.5",
    "phaser": "3.5.1"
  },
  "devDependencies": {
    "raw-loader": "^0.5.1",
    "webpack": "^4.6.0",
    "webpack-cli": "^2.0.14",
    "webpack-dev-server": "^3.1.3"
  },
  "name": "study-home-iot",
  "version": "1.0.0",
  "description": "iot",
  "author": "Adrien Soepriatna",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --mode=production",
    "build-dev": "webpack --mode=development",
    "start": "npm run build && webpack-dev-server --mode=production --port=8000",
    "start-dev": "npm run build && webpack-dev-server --mode=development --port=8000"
  },
  "keywords": [
    "iot"
  ]
}
